@CLAUDE.md

# Agent Entry Point

Any agent working in this repo — Claude Code, Codex, or otherwise — must read **both** of the following files before taking any action:

1. **`CLAUDE.md`** (repo root) — project conventions: commit/PR workflow, the 4 PO-locked directives, and links to architecture docs.
2. **`.claude/CLAUDE.md`** — agent onboarding: role definitions, gates, code style, architecture overview, and critical do-nots.

The `@CLAUDE.md` line above is for Claude Code's native include. Agents that do not resolve `@`-includes should follow the prose pointers above instead.
